
#ifndef CLASEREPASOMEMORIA_UTILS_HPP
#define CLASEREPASOMEMORIA_UTILS_HPP

#include <iostream>
#include <fstream>
#include <iomanip>
#include <cmath>
#include <cstring>
#define MAX_PALABRAS 300
#define INCREMENTO 5
enum REG_USER {CUENTA, NOMBRE, CORREO, FACULTAD};
enum REG_FE {FECHA, EDAD};
enum REG_ID_FEHCA {ID,FECHA_PUB};
enum REG_PUB {CUENTA_PUB, TEXTO};

using namespace std;

#endif //CLASEREPASOMEMORIA_UTILS_HPP